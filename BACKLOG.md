# BACKLOG - truestack

Seeded 2026-07-14 (main-thread audit; Gemini swarm was down). Web tech-stack fingerprinter (HTTP headers + body signals via an Aho-Corasick body matcher, rules from an embedded `rules.toml`). TLS-bypass is correctly gated behind `--insecure` with a loud warning (main.rs:56-58) — NOT a finding. Findings center on silent detection-disabling fallbacks (the recall-critical class). Rows: `file:line | category | severity | problem | acceptance criteria`.

DONE 2026-07-14 | src/fingerprints.rs:127 | silent-fallback | high | The body-pattern Aho-Corasick matcher fell back to an EMPTY matcher on build failure while `body_patterns` still held the real list — every body fingerprint silently stopped matching (Law 10 recall loss) with N patterns declared, 0 matchable. | FIXED: replaced the `unwrap_or_else(|_| empty matcher)` with a loud `panic!` naming the pattern count and error — a build failure is a Tier-B rule-data bug and truestack now refuses to run with a matcher that silently matches nothing (fail closed). Test `compiled_body_matcher_matches_its_patterns` asserts `body_ac.patterns_len() == body_patterns.len()`, the matcher matches its own pattern, and `detect_with_engine` finds the tech.
src/main.rs:77 | silent-fallback | medium | The response body is read with `resp.text().await.unwrap_or_default()` — a body-read error yields an empty string, so all body fingerprints silently see no content and the host is under-fingerprinted with no signal distinguishing "empty body" from "failed to read body." | surface a body-read failure (warn + mark the fingerprint partial) rather than treating it as an empty body; add a test for the read-error path.
src/fingerprints.rs:172 | robustness | low | Embedded rules parse with `.unwrap_or_else(|e| panic!("failed to parse embedded rules.toml: {e}"))` — a malformed embedded `rules.toml` panics the whole tool at first use rather than being caught by a compile-time/load-time test. | add a test that the embedded `rules.toml` parses (turning this into a build-time guarantee) so the panic is unreachable in practice; keep the panic only as a last-resort invariant.
src/html.rs:6 | robustness | low | `scraper::Selector::parse("title").ok()?` returns `None` on a parse failure of a STATIC selector that can never fail — masking a would-be-impossible error as "no title found." | use a `once`-compiled selector (built once, asserted valid) rather than parsing a literal per call and swallowing the impossible error.
